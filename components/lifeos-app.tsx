'use client'

import { useState } from 'react'
import { Bell, CalendarDays, Check, ChevronLeft, ChevronRight, CircleHelp, ClipboardCheck, FileUp, LayoutDashboard, ListChecks, Menu, MoreHorizontal, Plus, Search, Settings, Sparkles, Sun, UserRound, X } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { updateTaskStatus } from '@/lib/api'
import { cn } from '@/lib/utils'
import { AIImportPage } from '@/components/ai-import-page'
import { NotificationsPage as NotificationsFeaturePage } from '@/components/notifications-page'

const nav = [
  { href: '/dashboard', label: 'Dashboard', icon: LayoutDashboard },
  { href: '/calendar', label: 'Calendar', icon: CalendarDays },
  { href: '/tasks', label: 'Tasks', icon: ListChecks },
  { href: '/ai-import', label: 'AI Import', icon: Sparkles },
  { href: '/notifications', label: 'Notifications', icon: Bell, badge: 3 },
]

const events = [
  { time: '09:00', title: 'Product design review', type: 'Work', color: 'bg-primary' },
  { time: '11:30', title: 'Team standup', type: 'Work', color: 'bg-chart-2' },
  { time: '13:00', title: 'Lunch with Alex', type: 'Personal', color: 'bg-chart-3' },
  { time: '15:30', title: 'Deep work: roadmap', type: 'Focus', color: 'bg-chart-4' },
]
const tasks = [
  { title: 'Review Q3 product roadmap', project: 'Work', due: 'Today', priority: 'High', done: false },
  { title: 'Book dentist appointment', project: 'Personal', due: 'Tomorrow', priority: 'Medium', done: false },
  { title: 'Read chapter 4 of Atomic Habits', project: 'Learning', due: 'Fri, Aug 15', priority: 'Low', done: true },
  { title: 'Plan weekend grocery list', project: 'Home', due: 'Sat, Aug 16', priority: 'Medium', done: false },
]

function Brand() { return <div className="flex items-center gap-2.5"><div className="flex size-8 items-center justify-center rounded-xl bg-primary text-primary-foreground"><CalendarDays className="size-4" /></div><span className="text-lg font-semibold tracking-tight">LifeOS</span></div> }

function Sidebar({ path, onClose }: { path: string; onClose?: () => void }) {
  return <aside className="flex h-full w-64 shrink-0 flex-col border-r border-sidebar-border bg-sidebar p-4">
    <div className="flex items-center justify-between px-2 py-2"><Brand />{onClose && <Button variant="ghost" size="icon-sm" onClick={onClose} aria-label="Close navigation"><X /></Button>}</div>
    <div className="mt-8 flex flex-1 flex-col gap-1">
      <p className="px-3 pb-2 text-[11px] font-semibold uppercase tracking-wider text-muted-foreground">Workspace</p>
      {nav.map(({ href, label, icon: Icon, badge }) => <a key={href} href={href} className={cn('flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium text-muted-foreground transition-colors hover:bg-sidebar-accent hover:text-sidebar-accent-foreground', path === href && 'bg-sidebar-primary text-sidebar-primary-foreground hover:bg-sidebar-primary hover:text-sidebar-primary-foreground')}><Icon className="size-4" /><span className="flex-1">{label}</span>{badge && <span className="flex size-5 items-center justify-center rounded-full bg-primary/10 text-[11px] text-primary">{badge}</span>}</a>)}
      <div className="mt-8"><p className="px-3 pb-2 text-[11px] font-semibold uppercase tracking-wider text-muted-foreground">Manage</p><a href="/settings" className={cn('flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium text-muted-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground', path === '/settings' && 'bg-sidebar-primary text-sidebar-primary-foreground')}><Settings className="size-4" />Settings</a></div>
    </div>
    <div className="rounded-xl border border-sidebar-border bg-sidebar-accent/50 p-3"><div className="flex items-center gap-2"><div className="flex size-8 items-center justify-center rounded-full bg-primary/10 text-primary"><Sparkles className="size-4" /></div><div><p className="text-xs font-medium">Make time for what matters</p><p className="mt-0.5 text-[11px] text-muted-foreground">Your day, organized.</p></div></div></div>
  </aside>
}

function Topbar({ onMenu }: { onMenu: () => void }) { return <header className="flex h-16 items-center gap-4 border-b border-border bg-background/90 px-4 backdrop-blur md:px-8"><Button variant="ghost" size="icon-sm" className="md:hidden" onClick={onMenu} aria-label="Open navigation"><Menu /></Button><div className="relative hidden max-w-sm flex-1 md:block"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input className="h-9 w-full rounded-lg border border-input bg-muted/30 pl-9 pr-3 text-sm outline-none focus:border-ring focus:ring-2 focus:ring-ring/20" placeholder="Search anything..." /></div><div className="ml-auto flex items-center gap-2"><Button variant="ghost" size="icon-sm" aria-label="Help"><CircleHelp /></Button><a href="/notifications" className="relative flex size-8 items-center justify-center rounded-lg text-muted-foreground hover:bg-muted"><Bell className="size-4" /><span className="absolute right-1 top-1 size-1.5 rounded-full bg-primary" /></a><div className="ml-1 flex items-center gap-2 border-l border-border pl-3"><div className="flex size-8 items-center justify-center rounded-full bg-primary/10 text-xs font-semibold text-primary">JD</div><div className="hidden text-left sm:block"><p className="text-xs font-medium">Jordan Davis</p><p className="text-[11px] text-muted-foreground">Personal workspace</p></div></div></div></header> }

function Stat({ label, value, note, icon: Icon }: { label: string; value: string; note: string; icon: typeof Check }) { return <div className="rounded-xl border border-border bg-card p-4"><div className="flex items-center justify-between"><p className="text-sm text-muted-foreground">{label}</p><Icon className="size-4 text-muted-foreground" /></div><p className="mt-3 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{note}</p></div> }

function Dashboard() { return <div className="flex flex-col gap-6"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Tuesday, August 12, 2025</p><h1 className="mt-1 text-2xl font-semibold tracking-tight md:text-3xl">Good morning, Jordan</h1><p className="mt-1 text-sm text-muted-foreground">Here&apos;s your day at a glance.</p></div><Button><Plus data-icon="inline-start" />Add activity</Button></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Stat label="Today&apos;s progress" value="68%" note="4 of 6 activities complete" icon={Check} /><Stat label="Upcoming" value="12" note="Across the next 7 days" icon={CalendarDays} /><Stat label="Focus time" value="3h 20m" note="+18% from last week" icon={Sun} /><Stat label="Tasks done" value="24" note="This month" icon={ClipboardCheck} /></div><div className="grid gap-6 xl:grid-cols-[1.4fr_1fr]"><section className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-semibold">Today&apos;s schedule</h2><p className="mt-1 text-xs text-muted-foreground">Tuesday, August 12</p></div><a href="/calendar" className="text-sm font-medium text-primary hover:underline">View calendar</a></div><div className="flex flex-col">{events.map((event) => <div key={event.time} className="flex items-center gap-4 border-b border-border/70 px-5 py-4 last:border-0"><span className="w-12 text-xs font-medium text-muted-foreground">{event.time}</span><span className={cn('size-2 rounded-full', event.color)} /><div className="flex-1"><p className="text-sm font-medium">{event.title}</p><p className="mt-0.5 text-xs text-muted-foreground">{event.type} · 45 min</p></div><Button variant="ghost" size="icon-sm" aria-label={`More options for ${event.title}`}><MoreHorizontal /></Button></div>)}</div></section><section className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-semibold">Tasks for today</h2><p className="mt-1 text-xs text-muted-foreground">Keep the momentum going</p></div><a href="/tasks" className="text-sm font-medium text-primary hover:underline">View all</a></div><div className="flex flex-col p-2">{tasks.slice(0, 4).map((task) => <div key={task.title} className="flex items-start gap-3 rounded-lg p-3 hover:bg-muted/50"><div className={cn('mt-0.5 flex size-4 shrink-0 items-center justify-center rounded border', task.done ? 'border-primary bg-primary text-primary-foreground' : 'border-input')}>{task.done && <Check className="size-3" />}</div><div className="min-w-0 flex-1"><p className={cn('truncate text-sm font-medium', task.done && 'text-muted-foreground line-through')}>{task.title}</p><p className="mt-1 text-xs text-muted-foreground">{task.project} · {task.due}</p></div><span className={cn('rounded-full px-2 py-0.5 text-[10px] font-medium', task.priority === 'High' ? 'bg-destructive/10 text-destructive' : task.priority === 'Medium' ? 'bg-accent text-accent-foreground' : 'bg-muted text-muted-foreground')}>{task.priority}</span></div>)}</div></section></div></div> }

function CalendarPage() { const [view, setView] = useState('Week'); return <div className="flex flex-col gap-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Your time, your way</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Calendar</h1></div><div className="flex items-center gap-2"><Button variant="outline" size="sm"><ChevronLeft /><span className="hidden sm:inline">Previous</span></Button><Button variant="outline" size="sm">Today</Button><Button variant="outline" size="sm"><span className="hidden sm:inline">Next</span><ChevronRight /></Button></div></div><div className="flex flex-col rounded-xl border border-border bg-card"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border p-4"><div className="flex rounded-lg bg-muted p-1">{['Day','Week','Month','Agenda'].map((item) => <button key={item} onClick={() => setView(item)} className={cn('rounded-md px-3 py-1.5 text-xs font-medium text-muted-foreground', view === item && 'bg-background text-foreground shadow-sm')}>{item}</button>)}</div><Button size="sm"><Plus data-icon="inline-start" />New event</Button></div><div className="grid grid-cols-7 border-b border-border text-center text-xs"><div className="border-r border-border p-3 text-muted-foreground">GMT-5</div>{['Mon 11','Tue 12','Wed 13','Thu 14','Fri 15','Sat 16','Sun 17'].map((day, i) => <div key={day} className={cn('border-r border-border p-3 last:border-0', i === 1 && 'bg-primary/5 text-primary font-semibold')}>{day}</div>)}</div><div className="grid min-h-[430px] grid-cols-7"><div className="border-r border-border pt-4 text-center text-xs text-muted-foreground"><div className="mb-12">9 AM</div><div className="mb-12">12 PM</div><div className="mb-12">3 PM</div><div>6 PM</div></div>{['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map((day, i) => <div key={day} className={cn('relative border-r border-border bg-[linear-gradient(to_bottom,transparent_49px,var(--border)_50px)] bg-[length:100%_50px]', i === 1 && 'bg-primary/[0.025]')}><div className="absolute inset-x-2 top-16 rounded-md border border-primary/20 bg-primary/10 p-2 text-[11px] font-medium text-primary">{i === 1 ? 'Product design review' : i === 3 ? 'Gym session' : i === 4 ? 'Focus block' : ''}</div>{i === 1 && <div className="absolute inset-x-2 top-44 rounded-md border border-chart-3/20 bg-chart-3/10 p-2 text-[11px] font-medium text-foreground">Lunch with Alex</div>}</div>)}</div></div></div> }

function TasksPage() { const [done, setDone] = useState(tasks.map(t => t.done)); const [syncMessage, setSyncMessage] = useState(''); const toggleTask = async (index: number) => { const nextDone = !done[index]; setDone(current => current.map((value, taskIndex) => taskIndex === index ? nextDone : value)); try { await updateTaskStatus(`demo-${index + 1}`, nextDone ? 'COMPLETED' : 'PENDING'); setSyncMessage('Task synced'); } catch { setSyncMessage('Saved locally in this preview'); } }; return <div className="flex flex-col gap-6"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Stay on top of what matters</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Tasks</h1></div><Button><Plus data-icon="inline-start" />Add task</Button></div><div className="flex flex-col gap-3 rounded-xl border border-border bg-card p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input className="h-9 w-full rounded-lg border border-input bg-background pl-9 pr-3 text-sm outline-none" placeholder="Search tasks..." /></div><Button variant="outline" size="sm">All statuses</Button><Button variant="outline" size="sm">All priorities</Button></div><div className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-semibold">All tasks</h2><p className="mt-1 text-xs text-muted-foreground">{tasks.length} tasks in your workspace{syncMessage ? ` · ${syncMessage}` : ''}</p></div><Button variant="ghost" size="icon-sm" aria-label="More task options"><MoreHorizontal /></Button></div><div className="flex flex-col">{tasks.map((task, i) => <div key={task.title} className="flex items-center gap-3 border-b border-border/70 px-5 py-4 last:border-0"><button onClick={() => void toggleTask(i)} className={cn('flex size-5 shrink-0 items-center justify-center rounded-md border', done[i] && 'border-primary bg-primary text-primary-foreground')} aria-label={`Mark ${task.title} ${done[i] ? 'incomplete' : 'complete'}`}>{done[i] && <Check className="size-3" />}</button><div className="min-w-0 flex-1"><p className={cn('truncate text-sm font-medium', done[i] && 'text-muted-foreground line-through')}>{task.title}</p><p className="mt-1 text-xs text-muted-foreground">{task.project} · Due {task.due}</p></div><span className="hidden rounded-full bg-muted px-2 py-1 text-[11px] font-medium text-muted-foreground sm:block">{task.priority}</span><Button variant="ghost" size="icon-sm" aria-label="Task options"><MoreHorizontal /></Button></div>)}</div></div></div> }

function ImportPage() { const [showReview, setShowReview] = useState(false); return <div className="flex flex-col gap-6"><div><p className="text-sm text-muted-foreground">Turn plans into time</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">AI Schedule Import</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Upload a document, image, or paste text. LifeOS will help you turn it into an editable schedule draft.</p></div><div className="grid gap-6 xl:grid-cols-[1fr_1.1fr]"><section className="rounded-xl border border-border bg-card p-6"><div className="mb-6 flex items-center gap-2"><div className="flex size-8 items-center justify-center rounded-lg bg-primary/10 text-primary"><Sparkles className="size-4" /></div><div><h2 className="font-semibold">Import workflow</h2><p className="text-xs text-muted-foreground">Always review before saving</p></div></div><div className="flex flex-col gap-4">{['Upload document','AI processing','Review extracted schedule','Confirm & add to calendar'].map((step, i) => <div key={step} className="flex items-center gap-3"><div className={cn('flex size-7 items-center justify-center rounded-full text-xs font-semibold', i === 0 ? 'bg-primary text-primary-foreground' : 'bg-muted text-muted-foreground')}>{i + 1}</div><span className={cn('text-sm', i === 0 ? 'font-medium' : 'text-muted-foreground')}>{step}</span>{i < 3 && <div className="ml-1 h-5 border-l border-dashed border-border" />}</div>)}</div></section><section className="rounded-xl border border-dashed border-primary/40 bg-primary/[0.025] p-6"><div className="flex min-h-64 flex-col items-center justify-center text-center"><div className="flex size-14 items-center justify-center rounded-2xl bg-primary/10 text-primary"><FileUp className="size-6" /></div><h2 className="mt-4 font-semibold">Upload a schedule</h2><p className="mt-2 max-w-xs text-sm text-muted-foreground">Drop a PDF, image, or text file here, or choose one from your device.</p><Button className="mt-5" onClick={() => setShowReview(true)}><FileUp data-icon="inline-start" />Choose file</Button><p className="mt-3 text-[11px] text-muted-foreground">PDF, PNG, JPG, TXT up to 10 MB</p></div></section></div>{showReview && <section className="rounded-xl border border-border bg-card"><div className="flex flex-col justify-between gap-3 border-b border-border p-5 sm:flex-row sm:items-center"><div><div className="flex items-center gap-2"><h2 className="font-semibold">AI-generated draft</h2><span className="rounded-full bg-accent px-2 py-0.5 text-[10px] font-semibold text-accent-foreground">Review required</span></div><p className="mt-1 text-xs text-muted-foreground">Found 3 activities from &quot;weekly-plan.pdf&quot;</p></div><Button onClick={() => setShowReview(false)}>Confirm &amp; add to calendar</Button></div><div className="flex flex-col gap-3 p-5">{['Product design review','Team standup','Deep work: roadmap'].map((title, i) => <div key={title} className="grid gap-3 rounded-lg border border-border p-4 sm:grid-cols-[1fr_140px_120px_auto] sm:items-center"><div><p className="text-sm font-medium">{title}</p><p className="mt-1 text-xs text-muted-foreground">AI extracted · Work</p></div><input className="h-8 rounded-md border border-input bg-background px-2 text-xs" value={i === 0 ? 'Tue, Aug 12' : 'Wed, Aug 13'} readOnly /><input className="h-8 rounded-md border border-input bg-background px-2 text-xs" value={i === 0 ? '09:00 - 09:45' : '10:00 - 10:30'} readOnly /><Button variant="ghost" size="icon-sm" aria-label={`Remove ${title}`}><X /></Button></div>)}</div></section>}</div> }

function SimplePage({ title, eyebrow, children }: { title: string; eyebrow: string; children: React.ReactNode }) { return <div className="flex flex-col gap-6"><div><p className="text-sm text-muted-foreground">{eyebrow}</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">{title}</h1></div>{children}</div> }
function NotificationsPage() { return <SimplePage title="Notifications" eyebrow="Stay informed, not overwhelmed"><div className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-semibold">Inbox</h2><p className="mt-1 text-xs text-muted-foreground">3 unread notifications</p></div><Button variant="outline" size="sm">Mark all as read</Button></div>{['Your AI import is ready for review','Dentist appointment starts tomorrow','You completed your focus goal'].map((n, i) => <div key={n} className={cn('flex items-start gap-4 border-b border-border/70 p-5 last:border-0', i < 2 && 'bg-primary/[0.025]')}><div className="mt-1 flex size-8 items-center justify-center rounded-full bg-primary/10 text-primary"><Bell className="size-4" /></div><div className="flex-1"><p className="text-sm font-medium">{n}</p><p className="mt-1 text-xs text-muted-foreground">{i === 0 ? '2 minutes ago' : i === 1 ? '1 hour ago' : 'Yesterday'}</p></div>{i < 2 && <span className="mt-2 size-2 rounded-full bg-primary" />}</div>)}</div></SimplePage> }
function SettingsPage() { return <SimplePage title="Settings" eyebrow="Make LifeOS work for you"><div className="grid gap-6 lg:grid-cols-[220px_1fr]"><nav className="flex gap-1 overflow-x-auto lg:flex-col"><button className="whitespace-nowrap rounded-lg bg-primary px-3 py-2 text-left text-sm font-medium text-primary-foreground">Profile</button>{['Appearance','Notifications','Calendar','AI preferences','Privacy','Account'].map(x => <button key={x} className="whitespace-nowrap rounded-lg px-3 py-2 text-left text-sm text-muted-foreground hover:bg-muted hover:text-foreground">{x}</button>)}</nav><div className="rounded-xl border border-border bg-card p-6"><h2 className="font-semibold">Profile settings</h2><p className="mt-1 text-sm text-muted-foreground">Update your personal details and workspace identity.</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="flex flex-col gap-2 text-sm font-medium">First name<input className="h-10 rounded-lg border border-input bg-background px-3 text-sm font-normal outline-none focus:border-ring" defaultValue="Jordan" /></label><label className="flex flex-col gap-2 text-sm font-medium">Last name<input className="h-10 rounded-lg border border-input bg-background px-3 text-sm font-normal outline-none focus:border-ring" defaultValue="Davis" /></label><label className="flex flex-col gap-2 text-sm font-medium sm:col-span-2">Email address<input className="h-10 rounded-lg border border-input bg-background px-3 text-sm font-normal outline-none focus:border-ring" defaultValue="jordan@example.com" /></label></div><Button className="mt-6">Save changes</Button></div></div></SimplePage> }

export function LifeOSApp({ path = '/dashboard' }: { path?: string }) { const [mobileOpen, setMobileOpen] = useState(false); const page = path === '/calendar' ? <CalendarPage /> : path === '/tasks' ? <TasksPage /> : path === '/ai-import' ? <AIImportPage /> : path === '/notifications' ? <NotificationsFeaturePage /> : path === '/settings' ? <SettingsPage /> : <Dashboard />; return <div className="flex min-h-screen bg-background"><div className="hidden md:block"><Sidebar path={path} /></div>{mobileOpen && <div className="fixed inset-0 z-50 flex md:hidden"><div className="absolute inset-0 bg-foreground/20" onClick={() => setMobileOpen(false)} /><div className="relative"><Sidebar path={path} onClose={() => setMobileOpen(false)} /></div></div>}<div className="flex min-w-0 flex-1 flex-col"><Topbar onMenu={() => setMobileOpen(true)} /><main className="mx-auto w-full max-w-[1440px] flex-1 p-4 md:p-8">{page}</main></div></div> }

export function AuthPage({ register = false }: { register?: boolean }) { return <main className="flex min-h-screen items-center justify-center bg-muted/30 p-4"><div className="w-full max-w-md rounded-2xl border border-border bg-card p-8 shadow-sm"><Brand /><div className="mt-10"><h1 className="text-2xl font-semibold tracking-tight">{register ? 'Create your account' : 'Welcome back'}</h1><p className="mt-2 text-sm text-muted-foreground">{register ? 'Start organizing the life you want.' : 'Sign in to continue to your workspace.'}</p></div><div className="mt-6 flex flex-col gap-4"><label className="flex flex-col gap-2 text-sm font-medium">Email<input className="h-10 rounded-lg border border-input bg-background px-3 font-normal outline-none focus:border-ring" placeholder="you@example.com" /></label><label className="flex flex-col gap-2 text-sm font-medium">Password<input type="password" className="h-10 rounded-lg border border-input bg-background px-3 font-normal outline-none focus:border-ring" placeholder="••••••••" /></label><Button className="mt-2 h-10">{register ? 'Create account' : 'Sign in'}</Button></div><p className="mt-6 text-center text-sm text-muted-foreground">{register ? 'Already have an account? ' : 'New to LifeOS? '}<a className="font-medium text-primary hover:underline" href={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Create an account'}</a></p></div></main> }
