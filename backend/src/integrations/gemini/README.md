# Gemini integration boundary

Future Gemini calls belong here. They must return structured draft data to the
service layer; only an explicit user-confirmation workflow may persist it
through Prisma.
